# T-01 / T-02 / T-03 / T-04 完成度审计（截至 2026-05-16）

## 结论

截至 **2026-05-16**，T-01 ~ T-04 仍为 **部分完成（Partial）**，但已新增一批“可复用实现骨架”：

- T-02：新增 `omnilss.rs_jax`，提供 JAX JIT 的 `_jax_wls` 与 `_jax_penalized_wls` 数值内核，并补充一致性单测。  
- T-04：新增 `cg_fit_v2` API 入口与导出路由，`cg_fit` 已默认映射到 v2 入口（并已接入显式外循环实现）。  

这意味着“API 与数值基础层”前进了一步，但“完整外循环替代实现”尚未完成。

---

## 增量进展（相对 2026-05-15）

### T-02（RS JAX 化）

- [x] 新增 `rs_jax.py`：
  - `_jax_wls`（JAX `lstsq`）
  - `_jax_penalized_wls`（JAX 线性求解，含 ridge 稳定项）
- [x] 新增 `test_rs_jax_consistency.py`：
  - 与 NumPy `lstsq` 数值一致性测试
  - 惩罚 WLS 正规方程验证
- [ ] `rs_fit_jax` 外层 `while_loop` / 内层 `scan` 仍未接入生产入口

### T-04（CG 真实实现）

- [x] 新增 `algorithms/cg_algorithm_v2.py` 与 `cg_fit_v2` 入口。
- [x] `algorithms.__init__` 中 `cg_fit` 默认导向 `cg_fit_v2`，同时保留 `cg_fit_lbfgs` 别名。
- [x] `mixed_algorithm.py` 的 `algorithm="cg"` 路由已切到 `cg_fit_v2`。
- [x] 新增 `test_cg_algorithm_v2.py`（别名与基础运行测试）。
- [x] `cg_outer_step`（含跨导数修正）与独立 CG 外循环已接入 `cg_fit_v2`。

---

## 当前状态矩阵

| 任务 | 当前状态 | 关键证据 |
|---|---|---|
| T-01 Formula 语法完善 | Partial | 语法能力已扩展，但 R `model.matrix()` 系统对齐未闭环 |
| T-02 RS JAX 化 | Partial+ | 已有 JAX WLS/penalized WLS 内核与单测，主循环未 JAX 化 |
| T-03 Property Testing | Partial | 仍需扩大 family 覆盖并纳入 CI 强制门禁 |
| T-04 CG 真实实现 | Partial+ | API 路由完成，核心“跨导数驱动外循环”未落地 |

---

## 下阶段建议（保持 P0）

1. 先完成 `rs_fit_jax` 闭环（`scan + while_loop + step-halving fori_loop`）。
2. 实装 `cg_outer_step` 并把跨导数修正真正用于每轮参数更新。
3. 将 README 的性能与算法声明和“真实完成度”持续同步。
4. 为 CG/RS 新增与 R 的数值一致性回归测试，避免“仅 API 可用”的假完成。
