# T-01 / T-02 / T-03 / T-04 完成度审计（截至 2026-05-15）

## 结论

截至 **2026-05-15**，T-01 ~ T-04 **未全部完成**，当前状态为“部分完成（Partial）”。

- T-01：核心语法支持已落地，但缺少与 R `model.matrix()` 的系统数值对齐报告。  
- T-02：`jnp.linalg.lstsq` 已接入一条热路径，但 RS 仍存在大量 `np/jnp` 转换，且尚未切换 `jax.lax.while_loop`。  
- T-03：已新增 Hypothesis 测试样例，但覆盖面仍不足，且未确认 CI 作为强制门禁。  
- T-04：`_compute_cross_derivatives` 已被调用用于诊断摘要，但尚未形成“自实现 CG 外层循环 + 交叉导数修正”的完整替代实现。

---

## 任务逐项核对

## T-01 修复 Formula Parser 的交互项支持

- [x] `:` 交互项解析与设计矩阵支持（`x1:x2`）。
- [x] `*` 展开逻辑（`x1*x2 -> x1 + x2 + x1:x2`）。
- [x] `factor(x)` 哑变量展开（treatment coding，去基准组）。
- [ ] 与 R `model.matrix()` 输出做系统数值比对（尚未形成自动化基准报告）。
- [ ] 文档中明确“支持语法边界与不支持项”的用户级说明仍需补齐。

**状态：Partial**

## T-02 消除 RS 算法中的 np/jnp 频繁互转

- [ ] 完整梳理并标注全部转换点（未输出清单）。
- [ ] `compute_working_weights_and_response` 纯 JAX 化（当前仍为 NumPy 运算）。
- [x] IRLS WLS 求解已切到 `jnp.linalg.lstsq`（带 NumPy 回退）。
- [ ] `jax.lax.while_loop` 替换 Python for（未完成）。
- [ ] NO/GA/NBI 重新 benchmark 并验证 JIT 后收益（未提供新的产物链接）。
- [ ] README 性能表格与声明同步更新（未完成）。

**状态：Partial**

## T-03 补充 Property-Based Testing

- [ ] `hypothesis` 加入开发依赖清单（当前测试使用 `importorskip`，依赖约束未统一入文档/配置）。
- [ ] “每个分布族” `logpdf` 有限性测试（目前仅 NO 样例）。
- [ ] `p/q` 单调与逆映射完整验证（目前仅 NO 的单调性样例）。
- [ ] RS 收敛性 property test（未实现）。
- [ ] CI 强制纳入并定义随机策略（未完成）。

**状态：Partial（Early stage）**

## T-04 完成 CG 算法真实实现

- [x] `_compute_cross_derivatives` 已接入 `cg_fit()` 路径（用于诊断 summary）。
- [ ] 完整 CG 外层循环（使用交叉导数修正）自实现尚未替代当前委托路径。
- [ ] RS vs CG 在 BCT/SHASH 等分布的收敛速度对比报告缺失。
- [ ] README 中 CG 完成状态与证据链仍需同步。
- [ ] CG 增补测试仍需扩展到“交叉导数修正实际影响收敛轨迹”的层级。

**状态：Partial**

---

## 文档一致性更新要求（本次已执行）

- 将本审计文档加入 `docs/development/` 并在 MkDocs 导航暴露。
- 在优先级路线图中为 T-01~T-04 增加“当前状态”标记，防止误判为已完成。

## 下一步建议（按阻塞优先级）

1. **先闭环 T-02**：给出 RS 热路径转换清单、纯 JAX working weights/response、while_loop 版本与 benchmark 产物。  
2. **再闭环 T-01**：补 R `model.matrix()` 数值对齐测试（自动化）。  
3. **并行推进 T-03**：扩到多 family，并在 CI 强制执行。  
4. **最后闭环 T-04**：给出非委托式 CG 外层实现与对比报告。
